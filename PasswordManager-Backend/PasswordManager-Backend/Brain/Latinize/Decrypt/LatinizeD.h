#pragma once
#include <string>
#include <vector>
#include <iostream>
#include <map>

std::vector<std::string> Split(std::string str, std::string token);
std::string LatinToString(std::string input);
int LatinToNumber(std::string input);
